# Q2 — Documentation-vs-reality drift audit: wire batch + KT-A + KT-B

**Scope:** gossip @ `8fc0165` (branch `working-connection`), gossip-kt @ `6ee2b03`
(branch `feature/compaction`). Verified against code (`grep`/`Read`), `dart test`
(1254/0 green, matches ledger), `git log`/`git show`, and `gh pr view 14`
(MERGED). Read-only — no files edited.

Ledgers verified as claims-of-record:
- `gossip/.superpowers/sdd/2026-08-29-wire-codec-batch/progress.md`
- `gossip-kt/.superpowers/sdd/2026-08-29-kt-batch-a-repository-contract-v2/progress.md`
- `gossip-kt/.superpowers/sdd/2026-08-29-kt-batch-b-sync-depth/progress.md`

## Findings

| # | Doc | Claim | Reality | Severity |
|---|---|---|---|---|
| 1 | `docs/backlog/kt-normalize-twin-divergences.md` — "Partition primitive" row | Recommendation says "Homed in the campaign backlog's future scope — see [kt-wire-versioning-campaign.md]'s register." Traces back to KT-B ledger T7: "REGISTER ROWS for T8: (1) kt lacks partition primitive … campaign backlog." | `docs/backlog/kt-wire-versioning-campaign.md`'s "Campaign register" table has **no row** for the partition primitive (checked every row: PeerDirectory, authorship floor, paging/budgeting, rotation, materializer-rebuild, translator deletion, signing question, signed/unsigned mismatch — none is it). The item is routed to a home that doesn't hold it. | **Real** — a genuine "homeless" item; the only place it currently lives is the divergence-register row itself and the KT-B ledger, not the campaign doc its own cross-reference points to. |
| 2 | `docs/roadmap.md` (Kotlin port section, divergence-register line) | "register active, growing a row per review; two rows (`appendAll` phantom-stream registration, test-strength flow-backs) still need normalizing into Dart" | The register has (at least) two *older* rows with the same standing recommendation — "Frame dispatch" ("Dart should adopt a single dispatch seam…") and "Decode failure contract" ("Candidate for Dart to adopt") — neither counted in the roadmap's "two rows" tally. The true count of rows recommending Dart-side action is 4, not 2. | Minor — undercounts, doesn't misstate what's landed; the two named rows are correctly described as new/open, just not exhaustive. |
| 3 | `docs/superpowers/plans/2026-08-29-wire-codec-batch.md` and both gossip-kt batch plans (`2026-08-29-kt-batch-a-repository-contract-v2.md`, `2026-08-29-kt-batch-b-sync-depth.md`) | Plans use `- [ ]` checkbox tracking per their "for agentic workers" header. | All three plans are 100% complete per their ledgers, yet **zero** checkboxes are checked (76 unchecked in wire-codec-batch, 30 in KT-A-v2, 47 in KT-B — confirmed by grep). | Trivial / not actually drift — this is a **consistent convention across all three plans in both repos**: the ledger (`progress.md`), not the plan file, is the completion record. Flagging only because the audit brief asked; no action warranted. |
| 4 | `docs/superpowers/specs/2026-08-28-wire-versioning.md` §5.3, §7, §11 | Rollout steps 1–3 code-complete, step-3 gate green; §7 schemas normative; §11 decisions ruled and implemented. | Spot-checked against shipped code: `wire_types.dart` (marker table, `frameTypeOffset`, per-class diagnostics — exceeds spec's baseline, matches PR review MINOR-2 follow-up), `coordinator_config.dart`/`event_stream.dart` (version-aware cap docs ~7.4KB/~22KB, matches CHANGELOG and the MUST-FIX-1 fix), `sync_wire_emission.dart` (v1/v2 emission modules match §7.2/§7.3). `dart test` reproduces 1254/0, matching the ledger's "PR #14 FINAL STATE". gossip-kt has `PeerDirectory.kt`, `MembershipPeerDirectory.kt`, `PendingPullTracker.kt`, and `CoordinatorConfig.kt`'s `wireVersion: WireVersion = WireVersion.V1` all present as claimed. | No mismatch found — spec matches shipped code. |
| 5 | Spec §5.3 step-3 gate status | "No sender may flip to v2 before this step is green" — gate status should be recorded per R1 step 3's instruction ("Record in the plan-execution notes"). | Recorded exactly there: `progress.md` line 14, "R1: §5.3 STEP-3 GATE GREEN 2026-08-29". | No mismatch — recorded where its own instruction said to record it. |
| 6 | `docs/backlog/kt-wire-versioning-campaign.md` | Should reflect wire batch done, KT-A done, KT-B done, next KT-C; residual `_adoptClaimedAuthorshipFloor` (routed from wire batch's K4 review) should appear "at next docs touch." | Campaign register has the authorship-floor row marked **Closed** by Batch KT-B, exactly as promised. "Batch KT-B (2026-08-29) is complete… What remains… is KT-C/D/E." matches. Per-repo convention (roadmap.md's own header: "Priority and status live here only — never in a backlog file"), the campaign doc correctly does not restate step/phase status — that lives in `roadmap.md`, which is itself accurate (◐ High, in progress). | No mismatch. |
| 7 | `docs/backlog/kt-normalize-twin-divergences.md` — 8 rows harvested at T8 (commit `8fc0165`) | getOrPut defect → Dart fix queued; 3 test-strength flow-backs; event-dispatch rule; monitor-guarded-services rule; partition-primitive gap; quiescence → wire-efficiency; ingestion-guard convergence; merge-path-serialization verdict. | `git show 8fc0165 -- docs/backlog/kt-normalize-twin-divergences.md` shows exactly 1 row updated (pending-request expiry) + 7 new rows added = 8 rows touched, matching "8 rows written." All content matches the KT-B ledger's per-task "REGISTER ROW(S) for T8" notes verbatim. Cross-refs resolve except the partition-primitive one (see finding #1). | Real (via #1) / otherwise accurate. |
| 8 | `docs/backlog/health-minor-findings-sweep.md` | Should contain the queued Dart `getOrPut`-before-validation fix. | Present: "`appendAll` registers a phantom empty stream on a rejected batch… Surfaced by the Kotlin port's Batch KT-B review… fixed on the Kotlin side (`fb0cd32`)… Validate before the `getOrPut`, same fix shape." | No mismatch. |
| 9 | `docs/backlog/kt-port-wire-efficiency.md` | Should own the quiescence pin. | Present: "This includes translating Dart's quiescence test pin once the pacing mechanism it exercises exists on the Kotlin side… surfaced during the Kotlin port's Batch KT-B; see the twin-divergence register's quiescence row." | No mismatch. |
| 10 | `gossip-kt/docs/plans/2026-05-14-compaction.md` and `2026-08-29-kt-batch-a-repository-contract-v2.md` (T8 supersession notes) | T8 prescribes: append changelog entry 6 to the compaction plan; amend an out-of-scope bullet with a nested supersession note; add one Non-Goals-header supersession note to KT-A-v2; add a line-160 inline supersession note; do NOT edit lines 306/332/403/755 individually. | All present exactly as prescribed, verified by direct read: changelog entry 6 (line 852), the nested "(superseded: the floor went on the wire in the 2026-08-29 wire/codec batch…)" note (line ~872), KT-A-v2's Non-Goals header note (line 97) and line-160 inline note, and lines 306/332/403/755 correctly left unedited (still read "Interim — Batch KT-B moves this decision…", i.e., historically accurate as written, not stale). | No mismatch. |
| 11 | `packages/gossip/CHANGELOG.md` Unreleased | v1-mode payload cap disclosure (~7.4KB) and v2 (~22KB); wireVersion default v1; migration-relevant breaking changes enumerated. | Matches shipped code exactly (`coordinator_config.dart:127-128`, `event_stream.dart:73-76`, `sync_message_codec.dart:177` `maxEntryPayloadForBudget`). Comprehensive and accurate — functions as the migration checklist per project convention. | No mismatch. |
| 12 | `docs/roadmap.md` — wire/campaign items, `engine-transport-frame-capability` | Statuses should be internally consistent post-batch. | Wire-versioning line (◐ High) and KT-audit line (◐ Medium, now correctly citing "sync-path-depth batch (KT-B) shipped 2026-08-29 closing items 3/9/11") both check out against the ledgers. `engine-transport-frame-capability.md` makes no cap-figure claims contradicted by the v1/v2 cap change (it's about transport-declared ceilings generically, not a fixed number) — no drift introduced by this batch. | No mismatch (aside from finding #2, filed above under this same doc). |

## Summary of real/actionable findings

Only **one** genuinely homeless item was found: the kt "partition primitive"
recommendation (KT-B T7 → divergence register → claims a home in
`kt-wire-versioning-campaign.md`'s register table, which does not contain it).
Everything else scanned — 8 harvested divergence rows, 2 supersession-note
sets across 2 kt plan files, the CHANGELOG, the campaign register's
closed-item rows, the spec's §5.3/§7/§11 content against shipped code, and
the wire-codec-batch's own step-3 gate recording — checked out accurate. One
additional minor inaccuracy (roadmap undercounting actionable-for-Dart
divergence rows as "two" when it's four) is cosmetic. The zero-checked-boxes
pattern across all three plan files is a consistent, apparently intentional
convention (ledger-as-source-of-truth), not drift.
