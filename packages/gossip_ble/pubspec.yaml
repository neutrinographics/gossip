name: gossip_ble
description: Bluetooth Low Energy transport for gossip - handles peer discovery, connection management, and message delivery over BLE.
version: 0.1.0
repository: https://github.com/neutrinographics/gossip/tree/main/packages/gossip_ble
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.4
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  gossip:
    path: ../gossip
  bluetooth_low_energy_platform_interface: ^6.0.0
  meta: ^1.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  mocktail: ^1.0.4
